# Supabase: project URL + Secret key (server-only; all DB access via API).
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_SECRET_KEY=your-secret-key-here

# Anthropic Claude (server-only - for voice habit parsing)
ANTHROPIC_API_KEY=your-anthropic-api-key-here
# Optional: model id (default claude-sonnet-4-6). See https://docs.anthropic.com/en/api/models
# ANTHROPIC_MODEL_ID=claude-sonnet-4-6

# NextAuth (server-side)
NEXTAUTH_URL=http://localhost:3000
NEXTAUTH_SECRET=your-nextauth-secret
GOOGLE_CLIENT_ID=your-google-client-id
GOOGLE_CLIENT_SECRET=your-google-client-secret
# OAuth callback / OpenID request timeout in ms (default 10000). Increase on slow networks.
NEXTAUTH_OAUTH_REQUEST_TIMEOUT_MS=15000